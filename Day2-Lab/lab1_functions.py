#1.	Define the Function
def greet_host(hostname):
    """2. Prints a greeting message for the given hostname."""
    print(f"Hello, {hostname}! Welcome to the network.")

#3. Call the function with a sample hostname
greet_host("router1")
greet_host("switch-core")


